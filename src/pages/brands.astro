---
import Layout from '../layouts/Layout.astro'
import BrandSection from '../components/react/brandsPage/BrandSection'
import BrandNav from '../components/react/brandsPage/BrandNav'
import { brandsData } from '../data/brandsData'
---

<Layout title="Todas las Marcas - QuickPhoneFix">
  <div class="relative min-h-screen bg-linear-to-br from-blue-950 via-black to-purple-950">
      <a 
        href="/" 
        class="
        absolute
        top-2 left-2 z-50
        flex items-center gap-2
        px-4 py-2
        bg-slate-900/90 backdrop-blur-xl
        border border-slate-700 hover:border-cyan-500/50
        rounded-xl
        text-white font-semibold
        transition-all duration-300
        hover:scale-105
        shadow-lg hover:shadow-cyan-500/20
        cursor-pointer
        group
        ">
        <svg class="w-5 h-5 text-cyan-400 group-hover:translate-x-0.5 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"/>
        </svg>
        <span>Atras</span>
      </a>
    
    <section class="pt-32 pb-20 px-4 relative overflow-hidden ">
      <div class="absolute inset-0 overflow-hidden pointer-events-none">
        <div class="absolute top-20 left-10 w-72 h-72 bg-purple-500/10 rounded-full blur-3xl animate-pulse"></div>
        <div class="absolute bottom-20 right-10 w-96 h-96 bg-pink-500/10 rounded-full blur-3xl animate-pulse delay-1000"></div>
      </div>

      <div class="max-w-7xl mx-auto text-center relative z-10">
        <div class="inline-block mb-6">
          <span class="px-4 py-2 bg-purple-500/10 border border-purple-500/20 rounded-full text-purple-400 text-sm font-semibold backdrop-blur-sm">
            Más de 350 modelos disponibles
          </span>
        </div>
        
        <h1 class="text-5xl md:text-7xl lg:text-8xl font-black mb-6 bg-linear-to-r from-purple-400 via-pink-400 to-purple-400 bg-clip-text text-transparent animate-linear">
          Todas Nuestras Marcas
        </h1>
        
        <p class="text-xl md:text-2xl text-slate-400 mb-12 max-w-3xl mx-auto leading-relaxed">
          Reparamos las marcas más populares del mercado con <span class="text-purple-400 font-semibold">garantía</span> y <span class="text-pink-400 font-semibold">repuestos originales</span>
        </p>

        <div class="grid grid-cols-2 md:grid-cols-3 gap-6 max-w-4xl mx-auto">
          <div class="bg-slate-900/50 backdrop-blur-sm border border-slate-800 rounded-2xl p-6 hover:border-purple-500/50 transition-all duration-300">
            <div class="text-4xl font-black text-purple-400 mb-2">10+</div>
            <div class="text-sm text-slate-400">Marcas</div>
          </div>
          <div class="bg-slate-900/50 backdrop-blur-sm border border-slate-800 rounded-2xl p-6 hover:border-pink-500/50 transition-all duration-300">
            <div class="text-4xl font-black text-pink-400 mb-2">350+</div>
            <div class="text-sm text-slate-400">Modelos</div>
          </div>
          <div class="col-span-2 md:col-span-1 bg-slate-900/50 backdrop-blur-sm border border-slate-800 rounded-2xl p-6 hover:border-purple-500/50 transition-all duration-300">
            <div class="text-4xl font-black text-purple-400 mb-2">100%</div>
            <div class="text-sm text-slate-400">Garantía</div>
          </div>
        </div>

      </div>
    </section>

    <BrandNav brands={brandsData.map(b => b.name)} client:load />

    <section class="py-20 px-4 ">
      <div class="max-w-7xl mx-auto space-y-24">
        {brandsData.map((brand, index) => (
          <BrandSection
            id={brand.id}
            name={brand.name}
            subtitle={brand.subtitle}
            iconColor={brand.iconColor}
            models={brand.models}
            index={index}
            client:load
          />
        ))}
      </div>
    </section>
    
    <section class="p-4 pb-12 relative overflow-hidden">
      <div class="max-w-4xl mx-auto text-center relative z-10">
        <div class="bg-slate-900/50 backdrop-blur-xl border border-slate-800 rounded-3xl p-12 shadow-2xl">
          <div class="w-20 h-20 bg-linear-to-br from-purple-500 to-pink-500 rounded-2xl flex items-center justify-center mx-auto mb-8 rotate-3 hover:rotate-6 transition-transform">
            <svg class="w-10 h-10 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 12h.01M12 12h.01M16 12h.01M21 12c0 4.418-4.03 8-9 8a9.863 9.863 0 01-4.255-.949L3 20l1.395-3.72C3.512 15.042 3 13.574 3 12c0-4.418 4.03-8 9-8s9 3.582 9 8z"/>
            </svg>
          </div>
          
          <h2 class="text-4xl md:text-5xl font-black mb-6 text-white">
            ¿No encuentras tu modelo?
          </h2>
          
          <p class="text-xl text-slate-300 mb-10 leading-relaxed">
            No te preocupes, nuestro equipo está aquí para ayudarte.<br class="hidden md:block"/>
            Contáctanos y encontraremos la <span class="text-purple-400 font-semibold">solución perfecta</span> para tu dispositivo.
          </p>
          
          <div class="flex flex-col sm:flex-row gap-4 justify-center">
            <a 
              href="/contact" 
              class="group inline-flex items-center justify-center gap-3 bg-linear-to-r from-purple-600 to-pink-600 hover:from-purple-500 hover:to-pink-500 text-white px-8 py-4 rounded-full font-bold text-lg transition-all transform hover:scale-105 shadow-2xl hover:shadow-purple-500/50"
            >
              Contactar Ahora
              <svg class="w-5 h-5 group-hover:translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7l5 5m0 0l-5 5m5-5H6"/>
              </svg>
            </a>
            
            <a 
              href="tel:5491123380549" 
              aria-label="Llamar al 11 2338 0549"
              class="inline-flex items-center justify-center gap-3 bg-white/10 hover:bg-white/20 backdrop-blur-sm text-white px-8 py-4 rounded-full font-bold text-lg transition-all border border-white/20"
            >
              <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"/>
              </svg>
              Llamar Ahora
            </a>
          </div>
        </div>
      </div>
    </section>

  </div>
</Layout>

<style>
  @keyframes linear {
    0%, 100% { background-position: 0% 50%; }
    50% { background-position: 100% 50%; }
  }

  .animate-linear {
    background-size: 200% auto;
    animation: linear 4s ease infinite;
  }

  @keyframes pulse {
    0%, 100% { opacity: 0.4; transform: scale(1); }
    50% { opacity: 0.6; transform: scale(1.1); }
  }

  .delay-1000 {
    animation-delay: 1s;
  }

  html {
    scroll-behavior: smooth;
  }

  html {
    scroll-padding-top: 150px;
  }
</style>